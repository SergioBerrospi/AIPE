# AIPE Backend Dependencies

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Rate limiting
slowapi>=0.1.9

# LLM client (OpenAI-compatible, works with Groq)
openai>=1.50.0

# Database
asyncpg>=0.30.0

# Embeddings (CPU-only — no CUDA needed for single-query inference)
# This cuts ~2GB from the Docker image vs the default CUDA-bundled torch
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.6.0+cpu
sentence-transformers>=3.0.0

# Config
pydantic-settings>=2.5.0

# Utilities
python-dotenv>=1.0.0